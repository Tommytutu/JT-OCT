# Datasets

The 11 `.npz` files contain binary predictors `X`, integer labels `y`,
`feature_names`, and `label_names`. Load them with
`numpy.load(path, allow_pickle=False)`.

Dataset dimensions and available original-provider links are in `catalog.json`.
The original providers' attribution and usage terms apply.
